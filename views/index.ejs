<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TAS Attendance</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>
<body>

    <div class="container">
    <b>Whole squad attendance</b>
    <table class="table-bordered">
        <tr>
            <th>Date</th>
            <th>Attendies</th>
        </tr>
        <tr>
             <% All.forEach(element => { %>
                    <tbody>
                        <td> <%= element[0]  %> </td>
                        <td><% for( let index = 1; index < element.length; index++ ) { %>
                            <%= element[index] %> 
                        <% } %></td>
                    </tbody>
            <% }) %>
        </tr>    
    </table>
    <b>Alpha attendance</b>
    <table class="table-bordered">
        <tr>
            <th>Date</th>
            <th>Attendies</th>
        </tr>
        <tr>
             <% Alpha.forEach(element => { %>
                    <tbody>
                        <td> <%= element[0]  %> </td>
                        <td><% for( let index = 1; index < element.length; index++ ) { %>
                            <%= element[index] %> 
                        <% } %></td>
                    </tbody>
            <% }) %>
        </tr>    
    </table>
    <b>Bravo attendance</b>
    <table class="table-bordered">
        <tr>
            <th>Date</th>
            <th>Attendies</th>
        </tr>
        <tr>
             <% Bravo.forEach(element => { %>
                    <tbody>
                        <td> <%= element[0]  %> </td>
                        <td><% for( let index = 1; index < element.length; index++ ) { %>
                            <%= element[index] %> 
                        <% } %></td>
                    </tbody>
            <% }) %>
        </tr>    
    </table>
    <b>Charlie attendance(LMAO)</b>
    <table class="table-bordered">
        <tr>
            <th>Date</th>
            <th>Attendies</th>
        </tr>
        <tr>
             <% Charlie.forEach(element => { %>
                    <tbody>
                        <td> <%= element[0]  %> </td>
                        <td><% for( let index = 1; index < element.length; index++ ) { %>
                            <%= element[index] %> 
                        <% } %></td>
                    </tbody>
            <% }) %>
        </tr>    
    </table>
    <b>Delta attendance</b>
    <table class="table-bordered">
        <tr>
            <th>Date</th>
            <th>Attendies</th>
        </tr>
        <tr>
             <% Delta.forEach(element => { %>
                    <tbody>
                        <td> <%= element[0]  %> </td>
                        <td><% for( let index = 1; index < element.length; index++ ) { %>
                            <%= element[index] %> 
                        <% } %></td>
                    </tbody>
            <% }) %>
        </tr>    
    </table>
</div>
</body>
</html>